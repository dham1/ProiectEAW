{% extends 'base.html.twig' %}

{% block title %}UserAnimal{% endblock %}

{% block body %}
    {% include('base.html.twig') %}

    <h1>UserAnimal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_animal.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_animal_index') }}">back to list</a>

    <a href="{{ path('user_animal_edit', {'id': user_animal.id}) }}">edit</a>

    {{ include('user_animal/_delete_form.html.twig') }}
{% endblock %}
