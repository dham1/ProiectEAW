{% extends 'base.html.twig' %}

{% block title %}Edit Examination{% endblock %}

{% block body %}
    {% include('base.html.twig') %}
<div class="container" style="margin-top: 5%">
    <div style="max-width: 600px;">
        <h1>Edit Examination</h1>

        {{ include('examination/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('examination/_delete_form.html.twig') }}
        <a class="btn btn-info" href="{{ path('health_card_examination',{HealthCard: health_card_id}) }}">Back</a>

    </div>
</div>

{% endblock %}
