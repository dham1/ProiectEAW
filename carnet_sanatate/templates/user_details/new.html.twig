{% extends 'base.html.twig' %}

{% block title %}New UserDetails{% endblock %}

{% block body %}
    {% include('base.html.twig') %}
    <div class="container" style="margin-top: 5%">
        <div style="max-width: 600px;">
            <h1>Create new UserDetails</h1>

            {{ include('user_details/_form.html.twig') }}

            <a class="btn btn-info" href="{{ path('user_show',{id: app.user.id}) }}">Back</a>
        </div>
    </div>
{% endblock %}
