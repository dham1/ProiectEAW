{% extends 'base.html.twig' %}

{% block title %}UserDetails{% endblock %}

{% block body %}
    <h1>UserDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_detail.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ user_detail.FirstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ user_detail.LastName }}</td>
            </tr>
            <tr>
                <th>BirthDate</th>
                <td>{{ user_detail.BirthDate ? user_detail.BirthDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ user_detail.Address }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ user_detail.Phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_details_index') }}">back to list</a>

    <a href="{{ path('user_details_edit', {'id': user_detail.id}) }}">edit</a>

    {{ include('user_details/_delete_form.html.twig') }}
{% endblock %}
